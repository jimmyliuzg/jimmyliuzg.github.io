---
import SinglePageLayout from "../layouts/SinglePageLayout.astro";
import { getCollection } from "astro:content";

// Get all posts (writing + journal), sorted by date (newest first)
const writingPosts = (await getCollection("writing")).map((post) => ({
  ...post,
  type: "writing" as const,
}));

const journalPosts = (await getCollection("journal")).map((post) => ({
  ...post,
  type: "field-notes" as const,
}));

const allPosts = [...writingPosts, ...journalPosts].sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime(),
);

const allProjects = (await getCollection("projects")).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime(),
);

// Project categories for filtering
const categories = ["All", "Personal", "Experimental", "Technical"];
---

<SinglePageLayout title="Jimmy Liu — Technical Program Manager">
  <!-- ===== HERO SECTION ===== -->
  <section
    id="hero"
    class="pt-28 md:pt-36 pb-14 px-0"
  >
    <div class="max-w-3xl mx-auto w-full">
      <!-- Utrecht-style index label -->
      <span class="utrecht-index">Portfolio / Technical Program Manager</span>

      <!-- Main Headline -->
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-8 leading-tight">
        Hey, I'm <span class="text-accent dark:text-accent-dark">Jimmy Liu</span>
      </h1>

      <!-- Narrative Introduction -->
      <div class="prose-hero space-y-5 text-base md:text-lg leading-relaxed">
        <p class="opacity-90">
          I'm a <strong>Technical Program Manager</strong> working at
          <a
            href="https://www.nvidia.com/en-us/solutions/autonomous-vehicles/"
            target="_blank"
            rel="noopener"
            class="hero-link">NVIDIA</a
          >, where I orchestrate global data operations for autonomous vehicles.
          My work contributes to NCAP 5-star safety ratings and L2++ autonomy
          systems—including the upcoming
          <a
            href="https://blogs.nvidia.com/blog/drive-av-software-mercedes-benz-cla/"
            target="_blank"
            rel="noopener"
            class="hero-link">Mercedes-Benz 2026 CLA</a
          >.
        </p>

        <p class="opacity-85">
          I believe in building technology that genuinely serves people. Before
          NVIDIA, I shaped data collection pipelines at
          <a
            href="https://www.magicleap.com/"
            target="_blank"
            rel="noopener"
            class="hero-link">Magic Leap</a
          >
          and
          <a
            href="https://www.aboutamazon.com/news/devices/what-is-amazon-lab126"
            target="_blank"
            rel="noopener"
            class="hero-link">Amazon Lab126</a
          >.
        </p>

        <!-- Professional Path -->
        <p class="opacity-85">
          If you're interested in my professional journey, you can explore my
          <a href="#about" class="hero-link">experience timeline below</a>,
          download my
          <a href="/resume.pdf" target="_blank" class="hero-link">resume</a>, or
          connect with me on
          <a
            href="https://www.linkedin.com/in/jimmy-l-92b028112/"
            target="_blank"
            rel="noopener"
            class="hero-link">LinkedIn</a
          >.
        </p>

        <!-- Creative/Projects Path -->
        <p class="opacity-85">
          Outside of work, I love tinkering with <a
            href="#projects"
            class="hero-link">side projects</a
          >—from web apps to creative experiments. I keep these on
          <a
            href="https://github.com/jimmyliuzg"
            target="_blank"
            rel="noopener"
            class="hero-link">GitHub</a
          >
          if you'd like to browse the source or collaborate.
        </p>

        <!-- Contact/Lead Gen Path -->
        <p class="opacity-85">
          Want to work together, chat about autonomous vehicles, or just say
          hello?
          <a href="#contact" class="hero-link hero-link-emphasis"
            >Let's get in touch</a
          >.
        </p>
      </div>

      <!-- Find Me On Section -->
      <div class="mt-12 pt-5 border-t border-accent dark:border-accent-dark">
        <p class="text-sm font-medium opacity-70 mb-4 uppercase tracking-wider">
          Find me on
        </p>
        <div class="flex flex-wrap gap-x-6 gap-y-2 text-base">
          <a
            href="https://github.com/jimmyliuzg"
            target="_blank"
            rel="noopener"
            class="hero-link-subtle">GitHub</a
          >
          <a
            href="https://www.linkedin.com/in/jimmy-l-92b028112/"
            target="_blank"
            rel="noopener"
            class="hero-link-subtle">LinkedIn</a
          >
          <a href="mailto:jimmyliuzg@gmail.com" class="hero-link-subtle"
            >Email</a
          >
        </div>
      </div>
    </div>
  </section>

  <!-- ===== PROJECTS SECTION ===== -->
  <section
    id="projects"
    class="py-20 px-0 border-t border-accent dark:border-accent-dark"
  >
    <div class="max-w-6xl mx-auto">
      <div
        class="flex flex-col md:flex-row md:items-end md:justify-between mb-12"
      >
        <div>
          <span class="utrecht-index">01 / Projects</span>
          <h2 class="text-3xl md:text-4xl font-bold tracking-[0.01em]">Projects</h2>
        </div>

        <!-- Filter Buttons -->
        <div class="flex flex-wrap gap-2 mt-6 md:mt-0" id="project-filters">
          {
            categories.map((cat, index) => (
              <button
                class={`filter-btn px-3.5 py-1.5 transition-colors duration-300
                ${index === 0 ? "bg-accent dark:bg-accent-dark text-white dark:text-dark-bg border-accent dark:border-accent-dark" : "text-accent dark:text-accent-dark hover:bg-accent hover:text-white dark:hover:bg-accent-dark dark:hover:text-dark-bg"}`}
                data-category={cat.toLowerCase()}
              >
                {cat}
              </button>
            ))
          }
        </div>
      </div>

      <!-- Projects Grid -->
      <div
        id="projects-grid"
        class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"
      >
        {
          allProjects.length > 0 ? (
            allProjects.map((project) => {
              const projectDate = new Date(project.data.date);
              const isRecent =
                Date.now() - projectDate.getTime() < 90 * 24 * 60 * 60 * 1000;
              // Use category from frontmatter (defaults to 'personal' if not set)
              const category = project.data.category || "personal";

              return (
                <article
                  class="project-card group border border-accent/40 dark:border-accent-dark/40 bg-light-card dark:bg-dark-card p-6 hover:border-accent dark:hover:border-accent-dark"
                  data-category={category}
                >
                  {isRecent && (
                    <span class="utrecht-label-solid mb-3">
                      New
                    </span>
                  )}
                  <h3 class="text-xl font-bold mb-2 group-hover:underline">
                    <a
                      href={`/projects/${project.slug}`}
                      class="hover:opacity-100"
                    >
                      {project.data.title}
                    </a>
                  </h3>
                  <p class="text-sm opacity-80 mb-4 line-clamp-3">
                    {project.data.description}
                  </p>
                  {project.data.tags && project.data.tags.length > 0 && (
                    <div class="flex flex-wrap gap-2 mb-4">
                      {project.data.tags.slice(0, 3).map((tag: string) => (
                        <span class="utrecht-label">
                          {tag}
                        </span>
                      ))}
                    </div>
                  )}
                  <div class="flex gap-3 text-sm font-medium">
                    {project.data.demoUrl && (
                      <a
                        href={project.data.demoUrl}
                        target="_blank"
                        rel="noopener"
                        class="underline hover:opacity-70"
                      >
                        Demo →
                      </a>
                    )}
                    {project.data.githubUrl && (
                      <a
                        href={project.data.githubUrl}
                        target="_blank"
                        rel="noopener"
                        class="underline hover:opacity-70"
                      >
                        Source →
                      </a>
                    )}
                    <a
                      href={`/projects/${project.slug}`}
                      class="underline hover:opacity-70"
                    >
                      Details →
                    </a>
                  </div>
                </article>
              );
            })
          ) : (
            <div class="col-span-full py-12 border border-dashed border-accent/50 dark:border-accent-dark/50 text-center">
              <p class="text-lg opacity-70">Projects coming soon...</p>
            </div>
          )
        }
      </div>

      <!-- View all projects -->
      <div class="mt-10 text-center">
        <a href="/projects" class="underline font-medium hover:opacity-70">
          View all projects →
        </a>
      </div>

      <!-- CTA between sections -->
      <div class="mt-16 text-center">
        <p class="text-lg opacity-70 mb-4">Interested in working together?</p>
        <a href="#contact" class="btn-secondary">
          Let's Connect
          <svg
            class="w-4 h-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- ===== ABOUT SECTION ===== -->
  <section id="about" class="py-20 px-0 border-t border-accent dark:border-accent-dark">
    <div class="max-w-4xl mx-auto">
      <span class="utrecht-index">02 / About</span>
      <h2 class="text-3xl md:text-4xl font-bold tracking-[0.01em] mb-10">About</h2>

      <!-- Skills Grid -->
      <div class="mb-16">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-3">
          <span class="w-8 h-px bg-accent dark:bg-accent-dark"></span>
          Core Skills
        </h3>
        <ul class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-3">
          <li
            class="pl-4 border-l border-accent/40 dark:border-accent-dark/40 py-2 hover:border-accent dark:hover:border-accent-dark transition-colors"
          >
            ML Operations (MLOps)
          </li>
          <li
            class="pl-4 border-l border-accent/40 dark:border-accent-dark/40 py-2 hover:border-accent dark:hover:border-accent-dark transition-colors"
          >
            Data Lifecycle Management
          </li>
          <li
            class="pl-4 border-l border-accent/40 dark:border-accent-dark/40 py-2 hover:border-accent dark:hover:border-accent-dark transition-colors"
          >
            Python & SQL Automation
          </li>
          <li
            class="pl-4 border-l border-accent/40 dark:border-accent-dark/40 py-2 hover:border-accent dark:hover:border-accent-dark transition-colors"
          >
            ADAS / AV Infrastructure
          </li>
          <li
            class="pl-4 border-l border-accent/40 dark:border-accent-dark/40 py-2 hover:border-accent dark:hover:border-accent-dark transition-colors"
          >
            Agile & Scrum Methodologies
          </li>
          <li
            class="pl-4 border-l border-accent/40 dark:border-accent-dark/40 py-2 hover:border-accent dark:hover:border-accent-dark transition-colors"
          >
            Regulatory Standards (NCAP)
          </li>
        </ul>
      </div>

      <!-- Experience Timeline -->
      <div class="mb-16">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-3">
          <span class="w-8 h-px bg-accent dark:bg-accent-dark"></span>
          Experience
        </h3>
        <div class="space-y-8">
          <div class="pl-6 border-l border-accent dark:border-accent-dark relative">
            <div
              class="timeline-dot"
            >
            </div>
            <span class="text-xs font-mono uppercase tracking-[0.08em] text-accent dark:text-accent-dark">2024 — Present</span>
            <h4 class="text-lg font-bold mt-1">
              Technical Program Manager, ML Operations
            </h4>
            <p class="text-sm font-semibold opacity-80 mb-2">NVIDIA</p>
            <p class="text-sm opacity-80">
              Led end-to-end data delivery for Active Safety features,
              contributing to NCAP 5-star ratings on upcoming
              <a
                href="https://blogs.nvidia.com/blog/drive-av-software-mercedes-benz-cla/"
                target="_blank"
                rel="noopener"
                class="underline font-semibold">Mercedes-Benz 2026 CLA</a
              >. Improved labeling output quality from 30% yield to 90% via
              iterative process refinements.
            </p>
          </div>

          <div class="pl-6 border-l border-accent dark:border-accent-dark relative">
            <div
              class="timeline-dot opacity-70"
            >
            </div>
            <span class="text-xs font-mono uppercase tracking-[0.08em] text-accent dark:text-accent-dark">2022 — 2024</span>
            <h4 class="text-lg font-bold mt-1">
              Technical Program Manager, AV Data Operations
            </h4>
            <p class="text-sm font-semibold opacity-80 mb-2">NVIDIA</p>
            <p class="text-sm opacity-80">
              Managed global delivery of 13,000+ hours of driving data.
              Implemented P50/P90 uptime targets, yielding a 10% improvement in
              data output and reducing downtime by 30%.
            </p>
          </div>

          <div class="pl-6 border-l border-accent dark:border-accent-dark relative">
            <div
              class="timeline-dot opacity-50"
            >
            </div>
            <span class="text-xs font-mono uppercase tracking-[0.08em] text-accent dark:text-accent-dark">2021 — 2022</span>
            <h4 class="text-lg font-bold mt-1">
              Senior Data Collection Engineer
            </h4>
            <p class="text-sm font-semibold opacity-80 mb-2">Magic Leap</p>
            <p class="text-sm opacity-80">
              Re-developed data collection pipelines for new workstreams,
              improving efficiency by 40%. Developed custom Python applications
              for hardware configuration and automated data tagging.
            </p>
          </div>

          <div class="pl-6 border-l border-accent dark:border-accent-dark relative">
            <div
              class="timeline-dot opacity-30"
            >
            </div>
            <span class="text-xs font-mono uppercase tracking-[0.08em] text-accent dark:text-accent-dark">2020 — 2021</span>
            <h4 class="text-lg font-bold mt-1">Data Collection Specialist</h4>
            <p class="text-sm font-semibold opacity-80 mb-2">Amazon Lab 126</p>
            <p class="text-sm opacity-80">
              Managed collection efforts for Alexa Q&A projects including Amazon
              Astro, Amazon Ring, and Amazon Echo.
            </p>
          </div>
        </div>
      </div>

      <!-- Education -->
      <div class="mb-12">
        <h3 class="text-xl font-bold mb-6 flex items-center gap-3">
          <span class="w-8 h-px bg-accent dark:bg-accent-dark"></span>
          Education
        </h3>
        <div class="pl-6 border-l border-accent dark:border-accent-dark">
          <h4 class="text-lg font-bold">B.S. in Biological Sciences</h4>
          <p class="text-sm opacity-80">
            University of California, Santa Barbara (2018)
          </p>
        </div>
      </div>

      <!-- Resume CTA -->
      <div class="flex flex-wrap gap-4 mt-12">
        <a href="/resume.pdf" target="_blank" class="btn-secondary">
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
            ></path>
          </svg>
          Download Resume
        </a>
        <a href="#contact" class="btn-secondary"> Get in Touch </a>
      </div>
    </div>
  </section>

  <!-- ===== WRITING SECTION ===== -->
  <section
    id="writing"
    class="py-20 px-0 border-t border-accent dark:border-accent-dark"
  >
    <div class="max-w-4xl mx-auto">
      <span class="utrecht-index">03 / Field Notes</span>
      <h2 class="text-3xl md:text-4xl font-bold tracking-[0.01em] mb-10">Field Notes</h2>

      {
        allPosts.length > 0 ? (
          <div class="space-y-8">
            {allPosts.slice(0, 5).map((post) => {
              const postDate = new Date(post.data.date);
              const isRecent =
                Date.now() - postDate.getTime() < 90 * 24 * 60 * 60 * 1000;
              const formattedDate = postDate.toLocaleDateString("en-US", {
                year: "numeric",
                month: "short",
                day: "numeric",
              });
              const isJournal = post.type === "field-notes";
              const href = isJournal
                ? `/journal/${post.slug}`
                : `/writing/${post.slug}`;

              return (
                <article class="group border-l border-accent dark:border-accent-dark pl-6 py-4 hover:bg-accent/5 dark:hover:bg-accent-dark/5 transition-colors">
                  <div class="flex items-start justify-between gap-4">
                    <div class="flex-1">
                      <div class="flex items-center gap-3">
                        <span class="text-xs font-mono uppercase tracking-[0.08em] text-accent dark:text-accent-dark">
                          {formattedDate}
                        </span>
                        {isRecent && (
                          <span class="utrecht-label-solid">
                            New
                          </span>
                        )}
                        {isJournal && (
                          <span class="utrecht-label">
                            Field Notes
                          </span>
                        )}
                      </div>
                      <h3 class="text-xl font-bold mt-1 group-hover:underline">
                        <a href={href}>{post.data.title}</a>
                      </h3>
                      <p class="text-sm opacity-80 mt-2">
                        {post.data.description}
                      </p>
                    </div>
                    <a
                      href={href}
                      class="text-2xl opacity-40 group-hover:opacity-100 transition"
                    >
                      →
                    </a>
                  </div>
                </article>
              );
            })}
          </div>
        ) : (
          <div class="py-12 border border-dashed border-accent/50 dark:border-accent-dark/50 text-center">
            <p class="text-lg opacity-70 mb-4">Field Notes coming soon...</p>
            <p class="text-sm opacity-60">
              Automated dispatches from the workbench — check back soon.
            </p>
          </div>
        )
      }

      {
        allPosts.length > 5 && (
          <div class="mt-8">
            <a href="/journal" class="text-sm underline font-medium hover:opacity-70">
              Field Notes archive →
            </a>
          </div>
        )
      }
    </div>
  </section>

  <!-- ===== CONTACT SECTION ===== -->
  <section id="contact" class="py-20 px-0 border-t border-accent dark:border-accent-dark">
    <div class="max-w-2xl mx-auto">
      <span class="utrecht-index">04 / Contact</span>
      <h2 class="text-3xl md:text-4xl font-bold tracking-[0.01em] mb-6">Get in Touch</h2>

      <p class="text-lg opacity-80 mb-12">
        Let's work together! Feel free to reach out for collaborations,
        opportunities, or just to say hi.
      </p>

      <form
        action="https://api.web3forms.com/submit"
        method="POST"
        class="space-y-6"
      >
        <input
          type="hidden"
          name="access_key"
          value={import.meta.env.PUBLIC_WEB3FORMS_ACCESS_KEY}
        />

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label for="name" class="block text-sm font-semibold mb-2"
              >Name</label
            >
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full p-3.5 border border-accent/40 dark:border-accent-dark/40 bg-transparent rounded-none transition-colors"
              placeholder="Your name"
            />
          </div>
          <div>
            <label for="email" class="block text-sm font-semibold mb-2"
              >Email</label
            >
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full p-3.5 border border-accent/40 dark:border-accent-dark/40 bg-transparent rounded-none transition-colors"
              placeholder="your@email.com"
            />
          </div>
        </div>

        <div>
          <label for="subject" class="block text-sm font-semibold mb-2"
            >Subject</label
          >
          <input
            type="text"
            id="subject"
            name="subject"
            required
            class="w-full p-3.5 border border-accent/40 dark:border-accent-dark/40 bg-transparent rounded-none transition-colors"
            placeholder="What's this about?"
          />
        </div>

        <div>
          <label for="message" class="block text-sm font-semibold mb-2"
            >Message</label
          >
          <textarea
            id="message"
            name="message"
            rows="6"
            required
            class="w-full p-3.5 border border-accent/40 dark:border-accent-dark/40 bg-transparent rounded-none transition-colors resize-none"
            placeholder="Tell me more..."></textarea>
        </div>

        <button type="submit" class="btn-primary w-full md:w-auto">
          Send Message →
        </button>
      </form>

      <!-- Alternative Contact Methods -->
      <div class="mt-16 pt-6 border-t border-accent dark:border-accent-dark">
        <p class="text-sm opacity-70 mb-4">Or reach out directly:</p>
        <div class="flex flex-wrap gap-6">
          <a
            href="mailto:jimmyliuzg@gmail.com"
            class="flex items-center gap-2 text-sm font-medium hover:underline"
          >
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path
                d="M2.003 5.884L10 9.882l7.997-3.998A2 2 0 0016 4H4a2 2 0 00-1.997 1.884z"
              ></path>
              <path d="M18 8.118l-8 4-8-4V14a2 2 0 002 2h12a2 2 0 002-2V8.118z"
              ></path>
            </svg>
            Email
          </a>
          <a
            href="https://github.com/jimmyliuzg"
            target="_blank"
            rel="noopener"
            class="flex items-center gap-2 text-sm font-medium hover:underline"
          >
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path
                d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23a11.509 11.509 0 013.004-.404c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"
              ></path>
            </svg>
            GitHub
          </a>
          <a
            href="https://www.linkedin.com/in/jimmy-l-92b028112/"
            target="_blank"
            rel="noopener"
            class="flex items-center gap-2 text-sm font-medium hover:underline"
          >
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path
                d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"
              ></path>
            </svg>
            LinkedIn
          </a>
        </div>
      </div>
    </div>
  </section>
</SinglePageLayout>

<style>
  /* Line clamp for project descriptions */
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>

<script>
  // Project filtering
  const filterBtns = document.querySelectorAll(".filter-btn");
  const projectCards = document.querySelectorAll(".project-card");

  filterBtns.forEach((btn) => {
    btn.addEventListener("click", () => {
      const category = btn.getAttribute("data-category");

      // Update button styles
      filterBtns.forEach((b) => {
        b.classList.remove(
          "bg-accent",
          "dark:bg-accent-dark",
          "text-white",
          "dark:text-dark-bg",
        );
      });
      btn.classList.add(
        "bg-accent",
        "dark:bg-accent-dark",
        "text-white",
        "dark:text-dark-bg",
      );

      // Filter projects
      projectCards.forEach((card) => {
        const cardCategory = card.getAttribute("data-category");
        if (category === "all" || cardCategory === category) {
          (card as HTMLElement).style.display = "block";
          setTimeout(() => {
            (card as HTMLElement).style.opacity = "1";
            (card as HTMLElement).style.transform = "translateY(0)";
          }, 50);
        } else {
          (card as HTMLElement).style.opacity = "0";
          (card as HTMLElement).style.transform = "translateY(20px)";
          setTimeout(() => {
            (card as HTMLElement).style.display = "none";
          }, 300);
        }
      });
    });
  });
</script>
