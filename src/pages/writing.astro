---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const writingPosts = (await getCollection("writing")).map((post) => ({
  ...post,
  type: "writing" as const,
}));

const journalPosts = (await getCollection("journal")).map((post) => ({
  ...post,
  type: "field-notes" as const,
}));

const allPosts = [...writingPosts, ...journalPosts].sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime(),
);
---

<BaseLayout title="Writing — Jimmy Liu">
  <div class="max-w-3xl">
    <h1 class="text-4xl font-bold mb-4 text-link dark:text-link-dark">
      Writing
    </h1>
    <p class="text-lg opacity-80 mb-3">
      Thoughts on technology, building, and whatever else is on my mind.
    </p>
    <p class="text-sm opacity-70 mb-8">
      Daily dispatches live separately as
      <a href="/journal" class="underline font-medium">Field Notes →</a>
    </p>

    <!-- Filter Buttons -->
    <div class="flex flex-wrap gap-2 mb-10" id="writing-filters">
      <button
        class="filter-btn px-3.5 py-1.5 text-xs font-bold uppercase tracking-[0.08em] border transition-colors duration-300 bg-accent dark:bg-accent-dark text-white dark:text-dark-bg border-accent dark:border-accent-dark"
        data-filter="all"
      >
        All
      </button>
      <button
        class="filter-btn px-3.5 py-1.5 text-xs font-bold uppercase tracking-[0.08em] border transition-colors duration-300 border-accent/40 dark:border-accent-dark/40 text-accent dark:text-accent-dark hover:bg-accent hover:text-white dark:hover:bg-accent-dark dark:hover:text-dark-bg"
        data-filter="writing"
      >
        Writing
      </button>
      <button
        class="filter-btn px-3.5 py-1.5 text-xs font-bold uppercase tracking-[0.08em] border transition-colors duration-300 border-accent/40 dark:border-accent-dark/40 text-accent dark:text-accent-dark hover:bg-accent hover:text-white dark:hover:bg-accent-dark dark:hover:text-dark-bg"
        data-filter="field-notes"
      >
        Field Notes
      </button>
    </div>

    <!-- Posts List -->
    <div class="space-y-0" id="posts-list">
      {
        allPosts.map((post) => {
          const postDate = new Date(post.data.date);
          const formattedDate = postDate.toLocaleDateString("en-US", {
            year: "numeric",
            month: "long",
            day: "numeric",
          });
          const isJournal = post.type === "field-notes";
          const href = isJournal
            ? `/journal/${post.slug}`
            : `/writing/${post.slug}`;

          return (
            <article
              class="group border-l border-accent dark:border-accent-dark pl-6 py-6 hover:bg-accent/5 dark:hover:bg-accent-dark/5 transition"
              data-type={post.type}
            >
              <div class="flex items-start justify-between gap-4">
                <div class="flex-1">
                  <div class="flex items-center gap-3 mb-2">
                    <time class="text-sm font-mono opacity-70">
                      {formattedDate}
                    </time>
                    {isJournal && (
                      <span class="text-xs px-2 py-0.5 border border-accent/40 dark:border-accent-dark/40 opacity-70 font-medium">
                        Field Notes
                      </span>
                    )}
                  </div>
                  <h2 class="text-xl font-bold group-hover:underline">
                    <a href={href}>{post.data.title}</a>
                  </h2>
                  <p class="text-sm opacity-80 mt-2">
                    {post.data.description}
                  </p>
                </div>
                <a
                  href={href}
                  class="text-2xl opacity-40 group-hover:opacity-100 transition mt-2"
                >
                  →
                </a>
              </div>
            </article>
          );
        })
      }
    </div>
  </div>
</BaseLayout>

<script>
  const filterBtns = document.querySelectorAll(".filter-btn");
  const posts = document.querySelectorAll("[data-type]");

  filterBtns.forEach((btn) => {
    btn.addEventListener("click", () => {
      const filter = btn.getAttribute("data-filter");

      // Update button styles
      filterBtns.forEach((b) => {
        b.classList.remove(
          "bg-accent",
          "dark:bg-accent-dark",
          "text-white",
          "dark:text-dark-bg",
          "border-accent",
          "dark:border-accent-dark"
        );
        b.classList.add("border-accent/40", "dark:border-accent-dark/40");
      });
      btn.classList.add(
        "bg-accent",
        "dark:bg-accent-dark",
        "text-white",
        "dark:text-dark-bg",
        "border-accent",
        "dark:border-accent-dark"
      );
      btn.classList.remove("border-accent/40", "dark:border-accent-dark/40");

      // Filter posts
      posts.forEach((post) => {
        const type = post.getAttribute("data-type");
        if (filter === "all" || type === filter) {
          (post as HTMLElement).style.display = "block";
        } else {
          (post as HTMLElement).style.display = "none";
        }
      });
    });
  });
</script>
